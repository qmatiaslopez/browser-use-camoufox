# Temporary Agent Instructions

These instructions are temporary and were generated by the dloop runner for this run.

Before starting work, read the local `tasks/` directory. It contains the project plan, diary, and task tracker.

Use these files as the coordination source:

- `tasks/todo.md`: the implementation plan and task checklist.
- `tasks/diary.md`: current project notes, status, and important context.
- Any other relevant files under `tasks/`.

When choosing work, pick the next real pending task from `tasks/todo.md`. Ignore instructional examples or inline text that merely contains checkbox syntax.

After completing a task and passing its verification commands, update `tasks/todo.md` by changing that task from `- [ ]` to `- [x]`.

Before finishing, update `tasks/diary.md` with the current project state. Add concise notes about what changed, and remove or correct anything that is no longer true.

Do not leave completed work undocumented in `tasks/diary.md`.
